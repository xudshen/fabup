name: fabup
description: FAB version manager — download, install, and switch fab-cli and dartic-cli versions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  args: ^2.4.0
  http: ^1.2.0
  pub_semver: ^2.1.0
  path: ^1.8.0

dev_dependencies:
  test: ^1.24.0
